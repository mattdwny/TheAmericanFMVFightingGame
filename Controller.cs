using UnityEngine;
using System.Collections;

public class Controller {
	enum Buttons {B_PUNCH, B_KICK};
	enum Axis {A_HORIZONTAL, A_VERTICAL};
	float[] buttons;
	float[] axes;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
